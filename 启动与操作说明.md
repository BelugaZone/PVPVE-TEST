# PVPVE 搜打撤 — 启动与操作说明

## 启动方式

### 方式一：Unity 编辑器
1. 打开 Unity，加载项目 `E:\pro1`
2. 打开 `Assets/Scenes/Lobby.unity` 场景
3. 按 Play 进入游戏

### 方式二：打包 exe
1. 运行 `E:\pro1\build\Udemy course - Top-Down Shooter.exe`

---

## 登录流程

1. 启动后看到登录界面，有三个按钮：
   - **启动服务器** — 仅启动服务器（不开客户端，用于专用服）
   - **以主机登录** — 启动服务器 + 客户端（房主用这个）
   - **以客机登录** — 仅启动客户端，输入服务器 IP（默认 127.0.0.1）
2. 输入玩家 ID（任意字符串，如 `p1`）
3. 点击对应按钮登录

---

## 大厅（仓库）界面

登录后进入大厅，可看到两个面板：

### 仓库面板（上方）
- **左侧 仓库**：跨局持久存储，新玩家有初始装备（手枪、近战、弹药、医疗包）
- **右侧 配装**：装备栏（Primary/Secondary/Melee/Tactical）+ 背包栏
- **拖拽操作**：从仓库拖到配装栏 = 提交装备；从配装栏拖回仓库 = 取回

### 房间面板（下方）
- **创建房间** — 无房间时显示，点击创建（自动成为房主）
- **加入房间** — 有房间且非成员时显示
- **开始游戏** — 仅房主可见，点击开始对局
- **退出房间** — 成员可退出；房主退出则转让给最早加入的成员

房间最多容纳 **4 人**。

---

## 对局流程

1. 房主点击「开始游戏」→ 所有成员加载对战场景
2. 玩家以配装的装备进入对局（仓库中对应物品被移出）
3. 对局开始，180 秒倒计时

### 操作按键
| 按键 | 功能 |
|---|---|
| WASD | 移动 |
| 鼠标左键 | 射击 / 近战攻击 |
| 鼠标右键 | 精确瞄准 |
| R | 换弹 |
| 1/2/3/4 | 切换武器槽 |
| B | 打开/关闭背包 |
| F | 搜索尸体 / 拾取物品 |
| G | 投掷手雷 |
| L | 锁定目标 |

### 对局目标
- 击败 Boss 敌人获取「核心」道具
- 携带核心进入撤离区，停留 10 秒成功撤离
- 或等待倒计时结束

### 死亡与复活
- 死亡后掉落所有装备为可搜索尸体（留原地）
- 10 秒倒计时后自动在出生点复活
- 复活时取回尸体上未被搜刮的剩余装备
- 手无寸铁复活（如果装备全被搜刮）

---

## 对局结束

- **撤离成功** 或 **倒计时归零** → 结算分数排名（5 秒）
- 所有存活玩家的装备/背包状态保留为配装
- 自动返回大厅仓库界面
- 仓库不变（仅配装栏更新为撤离时的状态）
- 可继续配装、创建房间、开下一局

---

## 数据存储

- 服务器存档路径：`C:\Users\wenjianhu\AppData\LocalLow\DefaultCompany\Udemy course - Top-Down Shooter\server_players_data.json`
- 按玩家 ID 持久化：仓库物品、配装、血量
- 编辑器和打包 exe 共用同一存档文件
- 清除存档：删除上述 json 文件，或将其内容改为 `{"players":[]}`

---

## 多人测试

### 同机双开
1. Unity 编辑器作为主机：登录界面点「以主机登录」
2. 另一个 Unity 实例（Clone 项目）或打包 exe 作为客机：输入 IP `127.0.0.1`，点「以客机登录」

### 跨机测试
1. 主机端：启动服务器或以主机登录
2. 客机端：输入主机 IP 地址，以客机登录
3. 确保 7770 端口（Tugboat UDP）未被防火墙拦截

### Clone 项目注意事项
- Clone 项目的 `Lobby.unity` 场景必须与主项目一致（含 RoomManager 等场景对象）
- 如出现 SceneId 不匹配错误，在 Clone 项目中运行 `Tools → Fish-Networking → Utility → Reserialize NetworkObjects`

---

## 常见问题

| 问题 | 解决方案 |
|---|---|
| 登录后黑屏 | 大厅无摄像机，属正常现象，仓库 UI 是覆盖层 |
| 端口被占用 | 等待释放或重启 Unity/exe |
| 房间按钮不可点 | 检查 RoomManager 是否在 Lobby 场景中 |
| 配装没进局 | 确保在仓库拖拽提交了配装再开始 |
| 图标显示为色块 | 程序化占位图，真实图标在 `Resources/icon_set/` |
